<launch>
  <!-- set spawn_controllers flag (true / false) -->
  <arg name="spawn_controllers" default="true"/>

  <!-- set debug flag (true / false) -->
  <arg name="debug" default="false"/>

  <!-- load robot description -->
  <arg name="robot_description_file" default="$(find-pkg-share tuw_iwos_robot_description)/urdf/iwos.xml"/>

  <!-- load hardware interface param -->
  <!-- why? -->
  <!--rosparam file="$(find-package tuw_iwos_hardware)/config/interface/hardware_interface.yaml" value="$(command 'load')"/-->
  <!-- load controller description param -->
  <!-- why? -->
  <!--rosparam file="$(find-package tuw_iwos_hardware)/config/controller/controller.yaml" command="load"/-->

  <!-- load revolute (trinamic) setup description param -->
  <!-- why? -->
  <!-- rosparam param="trinamic_hardware_interface_setup" subst_value="True">
    "$(find-package tuw_iwos_hardware)/config/setup/revolute_setup.yaml"
  </rosparam -->

  <!-- load steering (dynamixel) setup description param -->
  <!-- why? -->
  <!-- rosparam param="dynamixel_hardware_interface_setup" subst_value="True">
    "$(find-package tuw_iwos_hardware)/config/setup/steering_setup.yaml"
  </rosparam -->

  <!-- set logging level to debug if flag is set -->
  <!-- TODO check how to do that correctly -->
  <!--group if="$(var debug)" >
    <node pkg="rosservice" exec="rosservice" name="set_logger_level_ros" args="call /tuw_iwos_hardware_node/set_logger_level '{logger: 'ros', level: 'DEBUG'}'"/>
    <node pkg="rosservice" exec="rosservice" name="set_logger_level_rosout" args="call /tuw_iwos_hardware_node/set_logger_level '{logger: 'rosout', level: 'DEBUG'}'"/>
  </group-->

  <node pkg="robot_state_publisher" exec="robot_state_publisher" name="robot_state_publisher">
    <param name="robot_description" value="$(command 'xacro $(var robot_description_file)')" />
  </node>


  <node pkg="controller_manager" exec="ros2_control_node" name="controller_manager">
    <param from="$(find-pkg-share tuw_iwos_hardware)/bringup/controller.yaml" />
  </node>

  <node pkg="controller_manager" exec="spawner" args="joint_state_broadcaster" /> <!-- TODO check how to do that correctly -->

  <!--node pkg="controller_manager" exec="spawner" args="joint_control --param-file $(find-pkg-share tuw_iwos_hardware)/bringup/controller.yaml" /-->
  <!-- node pkg="controller_manager" exec="spawner" args="placeholder_controller" / -->
</launch>
